
## Каталог резервных копий

Перед первым запуском версии 2.4.0 создайте каталог:

```bash
mkdir -p /opt/invite-mailer-backups
```

При необходимости укажите другой путь через `BACKUP_ROOT` в `.env` или переменных стека Portainer.
